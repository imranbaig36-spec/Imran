Resolved. Cause: `Map-FinBridgeDrives.ps1` was migrated from a GPO logon script running in user context to an Intune PowerShell script running as SYSTEM, but the script and delivery design were not updated for that execution context, so `\\finbridge-fs01\Finance` could not be reached at sign-in and drive `S:` was not assigned. Action: the failing SYSTEM-context Intune deployment was disabled or removed for the Finance target, drive mapping was restored through a user-context method, and successful user logon to the host was verified at 09:09 with no further issues reported. Preventive: require execution-context review for all GPO-to-Intune script migrations, treat mapped drives and other user-session artifacts as user-context actions by default, and enforce pilot validation plus rollback steps before broad deployment. User confirmed working.